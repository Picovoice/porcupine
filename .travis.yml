sudo: false

language: python
python:
  - "2.7"
  - "3.3"
  - "3.4"
  - "3.5"
  - "3.6"

# Blacklist
branches:
  except:
    -/.*/

# Whitelist
branches:
  only:
    -master

install:
  - pip install -r requirements.txt
script:
  - py.test
