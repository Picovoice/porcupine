name: Flutter

on:
  workflow_dispatch:
  push:
    branches: [ master ]
    paths:
      - 'demo/flutter/pubspec.yaml'
      - '.github/workflows/flutter.yml'
  pull_request:
    branches: [ master ]
    paths:
      - 'demo/flutter/pubspec.yaml'
      - '.github/workflows/flutter.yml'

defaults:
  run:
    working-directory: demo/flutter

jobs:
  build-android:
    name: Build Android demo
    runs-on: ubuntu-latest
    strategy:
      matrix:
        flutter-version: ['2.8.1', '3.0.4']

    steps:
    - uses: actions/checkout@v3

    - name: Set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'

    - name: Install Flutter ${{ matrix.python-version }}
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: ${{ matrix.flutter-version }}

    - run: flutter pub get

    - run: flutter build apk

  build-ios:
    name: Build iOS demo
    runs-on: mac-latest
    strategy:
      matrix:
        flutter-version: ['2.8.1', '3.0.4']
        architecture: x64

    steps:
    - uses: actions/checkout@v3

    - name: Install Flutter ${{ matrix.python-version }}
    - uses: subosito/flutter-action@v2
      with:
        flutter-version: ${{ matrix.flutter-version }}

    - run: flutter pub get

    - run: flutter build ios --release --no-codesign
